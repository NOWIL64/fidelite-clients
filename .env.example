PORT=3000
HOST=0.0.0.0

# file (local) or postgres (production fiable)
STORAGE_BACKEND=file

# Mettre votre URL PostgreSQL (Neon/Supabase/Render PG)
# Exemple:
# DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
DATABASE_URL=
